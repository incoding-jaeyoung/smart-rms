'use client';

import { Layout } from 'antd';

const { Header: AntHeader } = Layout;

const Header: React.FC = () => {
  return <AntHeader>{/* 헤더 내용 - 현재 비어있음 */}</AntHeader>;
};

export default Header;
